paths:
  artifact_root: artifacts/cpu_structure_bucket_only
  plot_root: results/cpu_structure_bucket_only/plots
  analysis_root: results/cpu_structure_bucket_only/analysis
  run_root: results/cpu_structure_bucket_only/runs

vf:
  structure_mode: bucket_only

analysis:
  study_id: cpu_structure_bucket_only
